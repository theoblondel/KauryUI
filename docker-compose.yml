version: '3'

services:
  kauryui:
    image: httpd:alpine
    container_name: kauryui
    ports:
      - "2053:80"
    restart: unless-stopped
    volumes:
      - ./public:/usr/local/apache2/htdocs/

